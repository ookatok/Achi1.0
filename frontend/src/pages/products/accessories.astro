---
import Layout from '../../layouts/Layout.astro';

// Fetch user session for token authorization if needed
let productsList = [] as any[];

try {
  const res = await fetch(`${BACKEND_URL}/api/products?categorySlug=accessories`);
  if (res.ok) {
    productsList = await res.json();
  }
} catch (err) {
  console.error('[NestJS API Error] Fetching accessories failed. Using mocks.', err);
}

// Fallback Mock items matching the premium aesthetic
const mockAccessories = [
  { id: 301, name: "Minimalist Leather Belt", price: "120.00", label: "Belt", imageUrl: "/assets/images/products/belt.png" },
  { id: 302, name: "Chic Acetate Sunglasses", price: "180.00", label: "Sunglasses", imageUrl: "/assets/images/products/sunglasses.png" },
  { id: 303, name: "Premium Canvas Tote Bag", price: "95.00", label: "Tote Bag", imageUrl: "/assets/images/bag-cat-women.png" },
  { id: 304, name: "Sterling Silver Chain Necklace", price: "150.00", label: "Necklace", imageUrl: "/assets/images/products/necklace.png" },
];

const displayProducts = productsList.length > 0 ? productsList : mockAccessories;
---

<Layout title="Accessories | ACHI Studio" description="Complete your look with ACHI's curated selection of minimalist accessories." showHeroHeader={true}>
  <!-- Accessories Hero Banner -->
  <section class="w-full relative h-[60vh] bg-[#2E312F] flex items-center justify-center text-white overflow-hidden">
    <div class="absolute inset-0 bg-cover bg-center" style="background-image: url('/assets/images/banners/accessories.png?v=2');"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent opacity-60"></div>
    
    <div class="relative w-full max-w-[1440px] mx-auto px-6 flex flex-col justify-end h-full pb-16">
      <div class="flex items-end justify-between w-full">
        <div>
          <span class="border border-white border-opacity-40 px-3 py-1 text-[10px] tracking-widest uppercase font-light rounded-full mb-4 inline-block">Curated Essentials</span>
          <h1 class="font-serif text-5xl sm:text-7xl font-bold tracking-tight uppercase leading-none">Accessories</h1>
        </div>
        <div class="hidden sm:block text-right text-[11px] tracking-[0.35em] text-gray-300 uppercase font-light">
          Complete Your Look
        </div>
      </div>
    </div>
  </section>

  <!-- Filter Bar Info -->
  <section class="border-b border-gray-100 bg-white">
    <div class="max-w-[1440px] mx-auto px-6 py-4 flex items-center justify-between text-[11px] uppercase tracking-wider font-light text-gray-400">
      <nav class="flex items-center gap-2">
        <a href="/" class="hover:text-brand-charcoal transition-colors">Home</a>
        <span>/</span>
        <a href="/products" class="hover:text-brand-charcoal transition-colors">Shop</a>
        <span>/</span>
        <span class="text-brand-charcoal">Accessories</span>
      </nav>
      <span>Showing {displayProducts.length} items</span>
    </div>
  </section>

  <!-- Product Grid -->
  <div class="max-w-[1440px] mx-auto px-6 py-16">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8">
      {displayProducts.map((product: any) => (
        <div class="flex flex-col justify-between p-6 bg-white border border-gray-100 rounded-sm product-card-hover group relative">
          <!-- Heart Favorite Button -->
          <button class="absolute top-6 right-6 text-gray-400 hover:text-brand-charcoal transition-colors focus:outline-none cursor-pointer z-20">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
            </svg>
          </button>

          <!-- Product Image Box -->
          <div class="aspect-[1/1] overflow-hidden flex flex-col items-center justify-center bg-gray-50 border border-gray-100/50 rounded-sm mb-6 relative group">
            {product.imageUrl ? (
              <img src={product.imageUrl} alt={product.name} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" />
            ) : (
              <span class="text-[9px] uppercase tracking-[0.2em] text-gray-400 text-center font-light leading-relaxed">
                [Image: {product.name}]
              </span>
            )}
          </div>

          <!-- Product Details -->
          <div>
            <h3 class="font-serif text-xs font-semibold tracking-widest text-brand-charcoal uppercase leading-tight">
              {product.name}
            </h3>
            <span class="text-xs text-brand-gold font-medium mt-2 block">${Number(product.price).toLocaleString()}</span>
          </div>

          <!-- Clickable Area Link -->
          <a href={`/products/${product.id}`} class="absolute inset-0 z-10" aria-label="View Details"></a>
        </div>
      ))}
    </div>
  </div>
</Layout>
